---
wave: 2
depends_on: ["14-01-PLAN.md"]
files_modified:
  - "README.md"
  - "CONTRIBUTING.md"
  - "package.json"
autonomous: true
requirements: EXT-01
---

# 14-02: Final Docs + VSIX Package

## Goal
Rewrite the README with full architecture coverage, create CONTRIBUTING.md, bump version to 1.0.0, and produce the final VSIX package.

## Tasks

### 1. Rewrite README.md

<read_first>
- README.md
- package.json (commands, settings)
- .planning/ROADMAP.md (feature list)
</read_first>

<action>
Rewrite `README.md` with the following structure:

1. **Hero Section**: Title, one-line tagline, badges (version, license, VS Code version).
2. **Architecture Diagram**: Mermaid diagram showing:
   - User → Sidebar UI → Router
   - Router → Conversational Engine (fast path)
   - Router → Orchestrator → BMAD Agents (plan/code path)
   - BMAD Agents → LLM Service → Providers (Gemini/Groq/Anthropic/OpenAI)
   - Cross-cutting: FileService, VectorStore, FeedbackService
3. **9-Phase SDLC Pipeline**: Table mapping each phase to the agent and service that implements it.
4. **Key Features**: Grouped by category:
   - AI Persona Debates (4→7 agents including SecurityPersona)
   - Self-Healing Code Generation (compile→fix→retry loop)
   - Streaming Markdown Chat
   - CI/CD Scaffold (GitHub Actions, Docker, K8s)
   - OpenTelemetry + Grafana Generation
   - OWASP Compliance + GDPR/HIPAA Flagging
   - Pre-Commit Secret Scanner
   - README Auto-Sync + Changelog
   - Sprint Auto-Planner
   - Voice-First Interface (Kokoro TTS, Whisper STT)
5. **Installation**: From VSIX, from source.
6. **Configuration**: Table of all `verno.*` settings with defaults.
7. **Commands**: Table of all commands with descriptions and shortcuts.
8. **Contributing**: Link to CONTRIBUTING.md.
9. **Release Notes**: 1.0.0 block summarizing all 14 phases.
10. **License**.
</action>

<acceptance_criteria>
- README.md contains a Mermaid architecture diagram.
- README.md documents all 16+ commands.
- README.md documents all 9 SDLC phases.
- README.md has installation instructions (from VSIX and from source).
</acceptance_criteria>

### 2. Create CONTRIBUTING.md

<action>
Create `CONTRIBUTING.md` with:
1. **Prerequisites**: Node 18+, VS Code 1.80+.
2. **Setup**: `git clone`, `npm ci`, `npm run compile`.
3. **Development**: `npm run watch`, F5 to launch Extension Host.
4. **Testing**: `npm test` runs Mocha tests in VS Code test electron.
5. **PR Process**: Fork → feature branch → PR → CI must pass.
6. **Code Style**: TypeScript strict, ESLint config.
7. **Architecture Overview**: Brief pointer to the Mermaid diagram in README.
</action>

<acceptance_criteria>
- `CONTRIBUTING.md` exists at project root.
- Contains build and test instructions.
</acceptance_criteria>

### 3. Version Bump to 1.0.0

<read_first>
- package.json
</read_first>

<action>
1. Change `"version": "0.2.0"` → `"version": "1.0.0"`.
2. Add release notes block to README for 1.0.0.
</action>

<acceptance_criteria>
- `package.json` version is `"1.0.0"`.
</acceptance_criteria>

### 4. Build VSIX Package

<action>
Run:
```bash
npm install -g @vscode/vsce
vsce package
```
Verify: `verno-1.0.0.vsix` is produced in project root.
</action>

<acceptance_criteria>
- File `verno-1.0.0.vsix` exists.
- `vsce package` exits with zero warnings (or only expected ones).
</acceptance_criteria>

## Verification
- `npm run compile` passes.
- `vsce package` produces valid VSIX.
- README Mermaid diagram renders correctly on GitHub (or locally with Markdown preview).
- CONTRIBUTING.md build steps work: `npm ci && npm run compile`.
