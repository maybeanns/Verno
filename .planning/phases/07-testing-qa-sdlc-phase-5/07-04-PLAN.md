# Plan 07-04: MockLLMService + Agent Unit Tests (TST-04)

## Objective
Establish actual inner unit tests for the Verno extension itself, targeting ≥80% logic coverage on critical Agents, ensuring the SDLC pipelines are robust and verifiable locally.

## Steps
1. **MockLLMService Implementation**:
   - Create `src/services/llm/MockLLMService.ts` implementing the `ILLMProvider` interface.
   - Support queued responses where tests can push deterministic string blocks (e.g. `mockLlm.pushResponse("```FILE: main.ts\ncode\n```")`).
2. **Test Scaffolding**:
   - Introduce Mocha/Chai framework hooks inside `package.json` for extension inner-testing.
   - Write `src/test/suite/agents/DeveloperAgent.test.ts`. Inject `MockLLMService` and assert that the file parsing semantics yield correct file buffers to `FileService` without invoking remote LLM tokens.
3. **CI Target Integration**:
   - Establish `npm run test:extension` specifically aimed at validating core logic offline.

## Files Modified
- `src/services/llm/MockLLMService.ts` (new)
- `src/test/suite/agents/DeveloperAgent.test.ts` (new)
- `package.json`

## Verification
- Running `npm run test` successfully traverses the agent files completely offline in exactly under 2 seconds.
