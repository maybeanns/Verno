# Phase 2, Plan 2: Mode Toggle UI

<objective>
To replace the generic 3-option dropdown (Plan/Code/Ask) with a deliberate, explicitly styled visual UI toggle mapping "Conversational" and "Development" modes. This firmly splits the conversational SDLC advisor from the standard development mode action loops.
</objective>

<requirements>
- MOD-02: Mode clearly communicated in UI (toggle: Conversational ↔ Development)
</requirements>

<must_haves>
- [x] Create a dedicated CSS `.mode-toggle` pill-style switcher.
- [x] Add HTML to `conversationTemplate.ts` swapping the `<select id="modeSelect">` logic for a custom interactive UI block.
- [x] Wire Javascript event listeners to switch `S.mode` and update visually when clicked.
- [x] Map "Conversational" to `'ask'` in the backend API calls.
- [x] Map "Development" to `'code'` (and encompass "plan") seamlessly to not break existing `extension.ts` downstream commands.
</must_haves>

<implementation_steps>
- Edit `conversationTemplate.ts`: HTML, CSS, JS layers.
</implementation_steps>

<verification>
- Verify TS compiler passes.
</verification>
